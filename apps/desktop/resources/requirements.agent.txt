# Agent Gateway Python dependencies
# Used by PromptHub to set up the shared agent-venv

fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0
httpx>=0.27.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# Memory / extraction
mem0ai>=0.1.0

# LLM provider SDKs (optional but commonly used)
openai>=1.30.0
# anthropic>=0.25.0   # uncomment if using Claude
# google-generativeai>=0.5.0  # uncomment if using Gemini
