interface:
  display_name: "spec-init"
  short_description: "Kick off a project with workflow, knowledge, verification, change-workspace, README, and AGENTS scaffolding."
  brand_color: "#0F766E"
  default_prompt: "Use $spec-init to kick off a new project with spec-first documentation, verification planning, and change-workspace scaffolding before implementation starts."

policy:
  allow_implicit_invocation: true
