# Verification Plan: __PROJECT_NAME__

> 更新时间：__DATE__
>
> 先定义如何证明正确，再开始大规模实现。

## 1. 测试目标

- [需要优先防住哪些高风险问题]
- [哪些用户路径最关键]

## 2. 测试层次

| 层次 | 目标 | 覆盖内容 | 工具 / 方式 |
|---|---|---|---|
| White-box Unit | [验证纯逻辑] | [规则 / 算法 / 转换 / 边界条件] | [待定] |
| Integration | [验证模块协作] | [接口 / 数据存取 / 消息] | [待定] |
| E2E | [验证关键用户流程] | [注册 / 创建 / 支付 / 提交] | [待定] |
| Performance | [验证性能目标] | [延迟 / 吞吐 / 大数据量 / 并发] | [待定] |
| Security | [验证安全边界] | [鉴权 / 权限 / 输入校验 / 敏感信息] | [待定] |

## 3. 需求到测试映射

| Test ID | Requirement | Test Type | 核心断言 |
|---|---|---|---|
| `TEST-001` | `FR-001` | Unit | [断言] |
| `TEST-002` | `FR-002` | Integration | [断言] |

要求：

- 每个高优先级 `FR-*` 至少映射到一种自动化测试
- 每个高风险需求至少映射到白盒测试、回归测试，必要时补性能或安全测试
- 如果某条需求暂时不能自动化验证，要写清原因和替代验证方式

## 4. 第一批红灯测试

1. [先写的失败测试 1]
2. [先写的失败测试 2]
3. [先写的失败测试 3]

## 5. 测试数据与夹具

- [需要哪些样例数据]
- [需要哪些 mock / fixture / seed]

## 5.1 白盒测试重点

- 核心分支和边界条件是什么
- 哪些内部状态转换最容易出错
- 哪些函数 / 类需要做精细白盒覆盖

## 5.2 性能测试计划

- 关键性能指标：[延迟 / 吞吐 / 内存 / CPU]
- 压测场景：[高并发 / 大输入 / 热点路径]
- 通过标准：[例如 p95 小于 X ms]

## 5.3 安全测试计划

- 鉴权与权限测试
- 输入校验与注入风险测试
- 敏感信息泄露检查
- 文件上传 / 下载 / 执行类风险检查（如适用）

## 6. 回归策略

- 哪些路径必须保留回归测试
- bug fix 后要补哪类测试
- 哪些变更需要完整回归

## 7. 测试执行建议

- 先从最小一条高优需求开始写失败测试
- 优先覆盖容易返工、状态转换复杂、边界多的路径
- 如果项目还没选定测试框架，先写测试思路和断言，再延后具体工具选择
- 不要先写实现再回头“补点测试”，测试应该驱动设计和实现边界

## 8. 完成定义

- 每个高优先级需求至少有一种自动化验证
- 关键路径的失败场景有覆盖
- bug 修复必须补回归测试
- 测试命令可以被稳定重复执行
- 白盒、性能、安全中与当前版本相关的测试要求已被显式满足或标明延期原因
