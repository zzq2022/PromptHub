# Requirements: __PROJECT_NAME__

> 更新时间：__DATE__
>
> 这份文档只写 what / why，不写 how。

## 1. 需求摘要

[用 3 到 5 句话说明这个项目最终要交付什么]

## 1.1 新手参考示例

如果你不知道怎么写“需求摘要”，可以先参考这种写法：

- “交付一个让用户能完成一个核心任务的产品，并能清楚看到当前状态、错误反馈和下一步动作。”
- “V1 先解决一个最常见的主场景，不追求一次覆盖所有角色、报表、配置和高级自动化能力。”

## 2. 用户故事

- `US-001` 作为 [用户]，我希望 [能力]，以便 [收益]
- `US-002` 作为 [用户]，我希望 [能力]，以便 [收益]

## 3. 功能需求

| ID | 描述 | 为什么需要 | 优先级 |
|---|---|---|---|
| `FR-001` | [用户能够...] | [业务价值] | H |
| `FR-002` | [用户能够...] | [业务价值] | M |

## 4. 非功能需求

| ID | 描述 | 度量方式 |
|---|---|---|
| `NFR-001` | [例如关键页面在 3 秒内可交互] | [如何测量] |
| `NFR-002` | [例如错误响应不泄露内部实现] | [如何测量] |

## 5. 验收标准

| ID | 对应需求 | 验收条件 |
|---|---|---|
| `AC-001` | `FR-001` | [当...时...应...] |
| `AC-002` | `FR-002` | [当...时...应...] |

## 6. 优先级与版本边界

| Requirement | 优先级 | 目标版本 | 说明 |
|---|---|---|---|
| `FR-001` | H | V1 | [为什么先做] |
| `FR-002` | M | V1 或 V2 | [为什么可以延后] |

## 6.1 范围裁剪助手

如果当前想法很大，先按下面方式裁剪：

| 内容类型 | 更适合放进 V1 | 更适合放进 V2 |
|---|---|---|
| 核心流程 | 一个必须跑通的主流程 | 多角色、多分支、多变体流程 |
| 数据与管理 | 最小必要的数据读写 | 报表、导出、审计后台、复杂配置 |
| 集成能力 | 一个最关键的外部依赖 | 多系统联动、开放平台、SDK |
| 体验增强 | 基本可用、可恢复、可测试 | 个性化、批量操作、自动化增强 |

默认建议：

- V1 先只保留一条完整 `FR -> DES -> TEST -> T` 交付链。
- 如果某个能力“没有它产品就不能成立”，放 V1。
- 如果某个能力“锦上添花但不影响验证价值”，优先放 V2 候选。

注意：

- 这只是帮助你控制范围，不代表 requirements 只能写最小一部分。
- 如果你已经明确要做完整需求，这里应该继续把角色、流程、异常场景、权限和质量要求补全，而不是只停在裁剪建议。

## 6.2 完整需求覆盖检查

- 是否覆盖了主要用户角色，而不是只写一个“默认用户”
- 是否覆盖了主流程和关键异常流程
- 是否写清关键数据 / 资源边界
- 是否写清权限、审计、性能、安全等非功能要求
- 是否写清哪些内容已经确认，哪些仍是 `[待确认]`

## 7. 范围外内容

- [当前版本不做的内容 1]
- [当前版本不做的内容 2]

## 8. 外部依赖与约束

- [依赖系统 / 数据源 / 平台限制]

## 9. 需求自检

- 每条 `FR-*` 都能被单独验证
- 每条高优需求至少对应一个 `AC-*`
- 这里没有写技术栈、数据库、组件划分、接口实现细节
- 读完这份文档，不看代码也知道“项目要做什么”

## 9.1 常见错误示例

错误写法：

- “用 React + PostgreSQL + Redis 做一个管理系统。”
- “先建三张表，再写登录接口。”

为什么错：

- 这已经在说实现方案和执行步骤，不是在定义交付目标。

更好的写法：

- “用户可以登录并查看自己的任务状态。”
- “系统可以创建、查看和更新核心记录，并给出明确反馈。”

## 10. 待确认

- [待确认]
- [待确认]

## 不该写进这里的内容

- 技术栈名称
- 表结构和字段设计
- 类名、函数名、文件名
- API 路径细节
- 组件拆分方案
