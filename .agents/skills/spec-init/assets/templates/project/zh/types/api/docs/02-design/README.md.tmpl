# Design: __PROJECT_NAME__

> 更新时间：__DATE__
>
> 这份文档解释 how，不再重复 why。

## 1. 设计目标

- 提供清晰、稳定、可演进的接口契约
- 优先保证认证、校验、错误响应和依赖调用边界清晰

## 2. 系统边界

- 输入：HTTP 请求、上游系统回调、内部触发事件
- 输出：JSON 响应、状态码、事件确认或异步任务触发
- 外部系统：认证服务、数据库、缓存、第三方 API 或消息系统
- 不在本系统内的内容：前端展示层、复杂报表与运营后台

## 3. 架构概览

[建议按“传输层 -> 领域服务 -> 数据访问 / 外部集成”描述调用链，并标出同步与异步边界。]

## 3.1 候选方案与用户确认

| 议题 | 方案 | 优点 | 代价 / 风险 | 推荐意见 | 是否已获用户确认 |
|---|---|---|---|---|---|
| API 架构形态 | 单体服务 | 交付快、维护简单 | 后续扩展边界可能变差 | 第一版推荐 | [是 / 否] |
| API 架构形态 | 多服务拆分 | 边界更清晰 | 初期复杂度更高 | 非必要不推荐第一版直接采用 | [是 / 否] |

如果这里仍然没有确认，不要把架构方向当作已拍板事实。

## 4. 模块设计

| ID | 模块 | 职责 | 对应需求 |
|---|---|---|---|
| `DES-001` | API Transport Layer | 接收请求、做认证与参数校验、返回统一响应 | `FR-001`, `FR-002` |
| `DES-002` | Domain Service 和 Repository | 执行业务规则、协调持久化和外部依赖 | `FR-001` |

## 5. 数据与状态

- 核心实体：请求 DTO、领域实体、响应 DTO、错误对象
- 关键字段：资源标识、业务状态、版本字段、请求追踪信息
- 状态转换：收到请求 -> 校验 -> 执行业务 -> 持久化 / 调用依赖 -> 返回成功或失败结果

## 6. 接口与契约

| 接口 | 输入 | 输出 | 错误场景 | 对应需求 |
|---|---|---|---|---|
| `[HTTP] POST /v1/resources` | JSON 请求体、认证凭证 | 资源标识、当前状态、追踪字段 | 认证失败、参数非法、依赖超时、冲突写入 | `FR-001`, `FR-002` |

## 7. 技术决策与权衡

- 决策：采用显式的 controller/service/repository 分层与统一错误模型
- 备选方案：把业务和数据访问直接写在 handler 中
- 为什么不选：会让认证、校验、错误处理和可测试性一起恶化

重要决策建议同步记录到 `docs/adr/`。

## 7.1 技术栈与基础设施

- 后端框架：[例如 FastAPI / Express / Go HTTP / Spring Boot]
- 数据存储：[数据库 / 缓存 / 队列]
- 测试栈：[单元 / 集成 / API E2E]
- 部署形态：[单体 / 多服务 / Serverless]

说明：

- 这里必须写清楚为什么这个栈和部署形态适合当前需求。
- 如果栈还没定，也要写候选方案、利弊和是否已向用户确认。

## 7.2 架构质量目标

- 可维护性：[如何控制边界和演进成本]
- 性能目标：[延迟 / 吞吐 / 资源占用]
- 安全目标：[鉴权、权限、输入校验、敏感信息保护]
- 可测试性：[白盒、集成、回归、性能、安全测试如何落地]

## 7.3 后端工程约定与数据规范

- API 路径规范：[版本前缀、资源命名、动词使用、批量接口规则]
- 请求 / 响应规范：[字段命名风格、分页格式、时间格式、幂等字段]
- 错误模型：[错误码、错误消息、字段级校验错误、trace id]
- 数据库约定：[库表命名、主键策略、外键策略、软删除规则]
- 表结构与索引：[核心表、唯一约束、查询路径、索引策略]
- migration 规范：[文件命名、回滚要求、向前兼容原则]
- 事务边界：[哪些操作必须同事务，哪些必须异步解耦]
- 日志与监控：[结构化日志字段、审计字段、指标、告警]
- 安全边界：[鉴权方式、权限模型、敏感字段脱敏、写操作保护]

说明：

- 这些约定应该在开始实现接口和表结构前先确定，避免每个接口各写各的。
- 如果数据库或错误模型还没定，要明确写候选方案和 `[待确认]`，不要把默认习惯当成团队规范。

## 8. 需求映射说明

逐条说明设计如何覆盖关键需求：

- `DES-001` 通过统一入口承接 `FR-001`、`FR-002`
- `DES-002` 通过业务规则和数据边界承接 `FR-001`
- 如果某条需求暂时没有设计承接，显式标出来

## 9. 风险与缓解

- 风险：接口契约和存储模型耦合过深，后续演进困难
- 缓解：在传输层和领域层之间保持 DTO 与内部模型分离

## 10. 追踪矩阵

| Requirement | Design |
|---|---|
| `FR-001` | `DES-001`, `DES-002` |
| `FR-002` | `DES-001` |

## 11. 设计自检

- 每条高优需求都能在这里找到承接它的设计项
- 这里在讲“怎么做”，而不是重复“为什么做”
- 这里没有把任务清单、排期和碎执行步骤混进来
- 关键风险和取舍已经被显式记录
- 技术栈、架构方向和质量目标已经写清楚
- 对存在分歧的关键方案，已经给出利弊并标明是否获得用户确认
- API 命名、错误模型、数据库约定、migration 和事务边界已经写清楚

## 12. 待确认

- 是否需要同步 API 之外的 webhook 或异步消费能力
- 错误模型是否需要跨多个服务统一
- 数据库命名、主键策略和 migration 约定是否已有团队规范

## 不该写进这里的内容

- 纯产品背景复述
- 太细的执行任务清单
- “先做什么后做什么”的排期细节
