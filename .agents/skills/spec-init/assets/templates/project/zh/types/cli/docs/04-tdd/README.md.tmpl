# Verification Plan: __PROJECT_NAME__

> 更新时间：__DATE__
>
> 先定义如何证明正确，再开始大规模实现。

## 1. 测试目标

- 优先防住命令解析错误、配置歧义和退出码不稳定
- 优先覆盖终端用户和自动化脚本都会依赖的核心命令路径

## 2. 测试层次

| 层次 | 目标 | 覆盖内容 | 工具 / 方式 |
|---|---|---|---|
| White-box Unit | 验证命令解析和纯逻辑 | 参数规则、配置合并、输出格式化、关键分支 | [待定，例如单元测试框架] |
| Integration | 验证命令与依赖协作 | 文件系统、远端 API、外部命令调用 | [待定，例如命令集成测试] |
| E2E | 验证真实命令执行结果 | shell 调用、标准输出、标准错误、退出码 | [待定，例如 CLI 端到端测试] |
| Performance | 验证性能目标 | 启动耗时、批量执行、长输出 | [待定，例如性能基准] |
| Security | 验证安全边界 | 参数注入、凭证处理、敏感信息输出 | [待定，例如安全检查与负向测试] |

## 3. 需求到测试映射

| Test ID | Requirement | Test Type | 核心断言 |
|---|---|---|---|
| `TEST-001` | `FR-001` | E2E | 用户按帮助信息执行命令时，可以稳定完成核心流程 |
| `TEST-002` | `FR-002` | Integration | 参数非法、配置缺失或依赖失败时，会输出可操作错误并返回非零退出码 |

要求：

- 每个高优先级 `FR-*` 至少映射到一种自动化测试
- 与当前版本相关的白盒、性能、安全测试必须进入计划
- 如果某条需求暂时不能自动化验证，要写清原因和替代验证方式

## 4. 第一批红灯测试

1. 用户按帮助信息执行命令后，核心流程没有被触发
2. 参数非法时，CLI 没有给出明确错误提示
3. 依赖失败时，退出码仍然返回成功或输出不可供脚本解析

## 5. 测试数据与夹具

- 准备合法参数、缺失参数、冲突参数三类命令样例
- 为文件系统、环境变量、远端接口准备稳定 fixture 和隔离目录

## 5.1 白盒测试重点

- 参数解析分支
- 配置优先级冲突
- 退出码映射逻辑

## 5.2 性能测试计划

- 关键性能指标：启动时间、批量命令执行时间
- 压测场景：大输入、重复执行、长输出
- 通过标准：[例如常见命令在 X 秒内开始输出]

## 5.3 安全测试计划

- 命令参数注入风险
- 环境变量和凭证输出风险
- 错误输出是否泄露敏感上下文

## 6. 回归策略

- 所有对外暴露的命令、子命令和关键 flag 都要保留回归测试
- 任何退出码、输出格式或配置优先级变更都必须补回归测试
- 涉及破坏性命令时，要保留 dry-run 或确认逻辑测试

## 7. 测试执行建议

- 先从首个必须可运行的命令失败测试开始
- 优先覆盖最可能被 shell 脚本依赖的输出和退出码路径
- 如果项目还没选定测试框架，先写测试思路和断言，再延后具体工具选择
- 不要先写实现再回头补测试；命令边界和错误行为应由测试先约束

## 8. 完成定义

- 每个高优先级需求至少有一种自动化验证
- 关键路径的失败场景有覆盖
- bug 修复必须补回归测试
- 测试命令可以被稳定重复执行
- 白盒、性能、安全要求已被显式检查或标明延期原因
