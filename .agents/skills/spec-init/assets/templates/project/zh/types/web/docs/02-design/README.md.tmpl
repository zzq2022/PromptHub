# Design: __PROJECT_NAME__

> 更新时间：__DATE__
>
> 这份文档解释 how，不再重复 why。

## 1. 设计目标

- 为关键页面提供低阻力的首个可完成流程
- 优先保证页面反馈清晰、状态可见、错误可恢复

## 2. 系统边界

- 输入：浏览器访问、页面跳转、表单输入、按钮点击
- 输出：页面渲染结果、交互反馈、通知提示、状态展示
- 外部系统：后端 API、认证系统、埋点或监控系统
- 不在本系统内的内容：复杂后台运营流程、重型离线计算

## 3. 架构概览

[建议按“页面壳层 -> 功能模块 -> 数据访问层”描述浏览器端结构，并标出关键状态流。]

## 3.1 候选方案与用户确认

| 议题 | 方案 | 优点 | 代价 / 风险 | 推荐意见 | 是否已获用户确认 |
|---|---|---|---|---|---|
| 页面交付模式 | SPA | 交互灵活、前端状态掌控更直接 | 首屏和 SEO 可能更弱 | 适合后台型产品 | [是 / 否] |
| 页面交付模式 | SSR / SSG / 混合模式 | 首屏、SEO 或内容分发更友好 | 构建和部署复杂度更高 | 适合内容型或首屏要求高的产品 | [是 / 否] |

如果这里仍然没有确认，不要把页面交付模式当作已拍板事实。

## 4. 模块设计

| ID | 模块 | 职责 | 对应需求 |
|---|---|---|---|
| `DES-001` | 页面壳层与路由入口 | 承载页面导航、布局骨架和首屏入口 | `FR-001` |
| `DES-002` | 关键交互流与状态反馈 | 处理表单、异步状态、错误提示和成功反馈 | `FR-001`, `FR-002` |

## 5. 数据与状态

- 核心实体：页面视图状态、表单草稿、远端数据快照
- 关键字段：加载状态、错误信息、用户输入、最近结果
- 状态转换：空态 -> 加载中 -> 成功 / 失败 -> 重试或继续下一步

## 6. 接口与契约

| 接口 | 输入 | 输出 | 错误场景 | 对应需求 |
|---|---|---|---|---|
| [页面动作] 提交关键表单 | 用户输入、页面上下文 | 成功反馈、状态刷新、下一步入口 | 校验失败、网络失败、权限失败 | `FR-001`, `FR-002` |

## 7. 技术决策与权衡

- 决策：优先采用清晰的页面分层和显式状态管理
- 备选方案：把所有逻辑直接堆在页面组件中
- 为什么不选：会导致交互、状态和错误处理难以维护

重要决策建议同步记录到 `docs/adr/`。

## 7.1 前端体验与视觉规范

- 目标终端与分辨率：[移动端 / 平板 / 桌面端；最小支持宽度；主要断点]
- 响应式策略：[哪些页面优先移动端，哪些优先桌面端，布局如何收缩]
- 色彩体系：[品牌主色、辅助色、背景色、文本色、成功 / 警告 / 错误语义色]
- 字体与排版：[字体族、标题层级、正文大小、行高、字重]
- 间距与栅格：[8pt / 4pt 系统、内容最大宽度、栅格列数]
- 组件规范：[按钮、输入框、表单、弹窗、表格、卡片、导航等是否复用统一组件]
- 交互状态：[hover、focus、disabled、loading、empty、error、success]
- 可访问性基线：[颜色对比度、键盘操作、焦点可见性、表单标签、语义结构]
- 资源规范：[图标风格、图片比例、插图或品牌素材约束]

说明：

- 这些规范应该在开始大规模页面实现前先定下来，避免“先写页面，后补规范”。
- 如果品牌或设计系统还没定，也要明确写 `[待确认]`，而不是让开发阶段随意发挥。

## 7.2 技术栈与交付基础

- 前端框架：[例如 React / Vue / Svelte / 原生方案]
- 路由方案：[例如 Next.js App Router / React Router / Vue Router]
- 状态管理：[局部状态 / 全局状态 / 服务端状态方案]
- 样式方案：[CSS Modules / styled-components / 原生 CSS]
- 组件基础：[自研组件 / 现有设计系统 / 第三方 UI 库]
- 测试栈：[单元 / 组件 / E2E]
- 交付形态：[SPA / SSR / SSG / Hybrid]

说明：

- 这里必须写清为什么这个栈适合当前产品，而不是只列库名。
- 如果组件体系或样式方案还没定，也要写候选方案、利弊和用户确认状态。

## 7.3 架构质量目标

- 可维护性：[页面、模块、组件边界如何保持清晰]
- 性能目标：[首屏时间、交互延迟、包体积、图片与资源策略]
- 安全目标：[前端输入校验、权限展示边界、敏感信息处理]
- 可测试性：[白盒、组件、E2E、性能、安全测试如何落地]

## 8. 需求映射说明

逐条说明设计如何覆盖关键需求：

- `DES-001` 通过页面壳层和路由入口承接 `FR-001`
- `DES-002` 通过交互流和状态反馈承接 `FR-001`、`FR-002`
- 如果某条需求暂时没有设计承接，显式标出来

## 9. 风险与缓解

- 风险：页面状态分散导致交互不一致
- 缓解：统一关键状态命名和错误反馈模式

## 10. 追踪矩阵

| Requirement | Design |
|---|---|
| `FR-001` | `DES-001`, `DES-002` |
| `FR-002` | `DES-002` |

## 11. 设计自检

- 每条高优需求都能在这里找到承接它的设计项
- 这里在讲“怎么做”，而不是重复“为什么做”
- 这里没有把任务清单、排期和碎执行步骤混进来
- 关键风险和取舍已经被显式记录
- 分辨率、断点、颜色、字号、组件和交互状态规范已经写清楚
- 前端技术栈、组件体系和交付模式已经写清楚

## 12. 待确认

- 页面是以单页应用、服务端渲染还是混合模式交付
- 哪些状态需要跨页面共享
- 品牌色、字体和组件体系是否已有既定规范

## 不该写进这里的内容

- 纯产品背景复述
- 太细的执行任务清单
- “先做什么后做什么”的排期细节
