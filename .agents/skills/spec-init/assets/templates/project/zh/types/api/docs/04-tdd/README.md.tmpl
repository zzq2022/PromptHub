# Verification Plan: __PROJECT_NAME__

> 更新时间：__DATE__
>
> 先定义如何证明正确，再开始大规模实现。

## 1. 测试目标

- 优先防住契约破坏、认证校验失效和依赖集成回归
- 优先覆盖调用方最早接入的核心 HTTP 路径

## 2. 测试层次

| 层次 | 目标 | 覆盖内容 | 工具 / 方式 |
|---|---|---|---|
| Unit | 验证纯业务规则 | 参数规则、状态转换、错误映射 | [待定，例如单元测试框架] |
| Integration | 验证接口与依赖协作 | handler、存储、外部服务或消息系统 | [待定，例如接口集成测试] |
| E2E | 验证真实 HTTP 契约 | 请求、响应、状态码、认证头、错误结构 | [待定，例如 API 端到端测试] |

## 3. 需求到测试映射

| Test ID | Requirement | Test Type | 核心断言 |
|---|---|---|---|
| `TEST-001` | `FR-001` | E2E | 调用方按契约请求时，可以稳定获得成功响应和核心资源状态 |
| `TEST-002` | `FR-002` | Integration | 参数非法、认证失败和依赖异常时，接口返回统一错误结构 |

要求：

- 每个高优先级 `FR-*` 至少映射到一种自动化测试
- 如果某条需求暂时不能自动化验证，要写清原因和替代验证方式

## 4. 第一批红灯测试

1. 合法请求无法创建或查询核心资源
2. 缺失认证或参数非法时，没有返回统一错误结构
3. 上游依赖超时后，接口暴露了内部错误细节或返回不稳定状态码

## 5. 测试数据与夹具

- 准备最小合法请求、越界请求和冲突请求样例
- 为数据库、缓存、外部 API 或消息系统准备可复用 fixture

## 6. 回归策略

- 所有外部公开 API 都要保留契约回归测试
- 任何认证、校验、错误模型变更都必须补对应回归测试
- 涉及数据模型迁移、状态码变更或集成边界的改动需要完整回归

## 7. 测试执行建议

- 先从最小一条读写接口的失败测试开始
- 优先覆盖最先开放给调用方的端点和最难回滚的错误场景
- 如果项目还没选定测试框架，先写测试思路和断言，再延后具体工具选择

## 8. 完成定义

- 每个高优先级需求至少有一种自动化验证
- 关键路径的失败场景有覆盖
- bug 修复必须补回归测试
- 测试命令可以被稳定重复执行
