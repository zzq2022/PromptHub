# Requirements: __PROJECT_NAME__

> 更新时间：__DATE__
>
> 这份文档只写 what / why，不写 how。

## 1. 需求摘要

[交付一个以命令行为主要入口的工具，让用户能通过命令、参数和配置快速完成核心任务，并得到清晰输出与可判断的退出结果。]

## 2. 用户故事

- `US-001` 作为终端用户，我希望通过一条明确的命令完成核心操作，以便提升日常效率
- `US-002` 作为自动化脚本维护者，我希望工具输出稳定、退出码明确，以便把它接入现有流水线或 shell 脚本

## 3. 功能需求

| ID | 描述 | 为什么需要 | 优先级 |
|---|---|---|---|
| `FR-001` | 用户能够通过命令、参数和配置触发核心流程 | 让工具真正可用于终端操作和自动化脚本 | H |
| `FR-002` | 工具能够提供清晰输出、错误提示和非零退出码 | 让用户和脚本都能稳定判断执行结果 | H |

## 4. 非功能需求

| ID | 描述 | 度量方式 |
|---|---|---|
| `NFR-001` | 常用命令应在本机环境下快速开始输出结果 | 测量启动耗时和首个可见输出时间 |
| `NFR-002` | 默认输出适合终端阅读，并可为自动化场景保留稳定格式 | 通过人工验证和脚本解析验证输出稳定性 |

## 5. 验收标准

| ID | 对应需求 | 验收条件 |
|---|---|---|
| `AC-001` | `FR-001` | 当用户按帮助信息输入命令和参数时，应能稳定执行核心流程 |
| `AC-002` | `FR-002` | 当执行失败时，应输出可操作的错误提示并返回非零退出码 |

## 6. 优先级与版本边界

| Requirement | 优先级 | 目标版本 | 说明 |
|---|---|---|---|
| `FR-001` | H | V1 | 没有可执行的命令入口，CLI 项目无法成立 |
| `FR-002` | H | V1 | 输出和退出码直接决定 CLI 是否可自动化使用 |

## 7. 范围外内容

- 图形化 Web 管理界面
- 长期运行的后台调度服务

## 8. 外部依赖与约束

- Shell 环境和目标操作系统
- 本地配置文件、环境变量或凭证管理方式
- 远端 API、文件系统或外部命令依赖

## 8.1 新手填写提示

如果你还不知道怎么写，可以先回答下面这些问题：

| 问题 | 为什么要先确认 |
|---|---|
| 第一个必须可用的命令和参数组合是什么？ | 决定 V1 的核心价值 |
| 主要是给人工使用，还是需要接入自动化流水线？ | 会影响输出格式和退出码要求 |
| 是否需要 JSON 等机器可读输出？ | 会影响需求和验收标准 |
| 是否会执行删除、覆盖、发布等破坏性操作？ | 会影响确认机制和安全边界 |
| 目标操作系统和 shell 是什么？ | 会影响兼容性要求 |

## 8.2 可直接参考的 V1 示例

如果你做的是常见自动化工具，可以先参考：

- V1 命令：只做一个最常用命令和一组最小必要参数
- V1 输出：明确 stdout / stderr、成功 / 失败退出码、`--help`
- V1 安全：对删除、覆盖、发布等操作要求确认或 dry-run
- V2 候选内容：交互式 UI、批量子命令、插件系统、远程同步

## 9. 需求自检

- 每条 `FR-*` 都能被单独验证
- 每条高优需求至少对应一个 `AC-*`
- 这里没有写技术栈、数据库、组件划分、接口实现细节
- 读完这份文档，不看代码也知道“项目要做什么”

## 10. 待确认

- 首个必须支持的命令和参数组合是什么
- 是否需要机器可读输出格式，例如 JSON
- 是否会执行潜在破坏性操作，以及确认机制如何设计

## 10.1 常见错误示例

错误写法：

- “用 Go 写一个 CLI，先加 Cobra，再做 deploy 子命令。”

更好的写法：

- “用户可以通过命令触发一次发布检查，并明确知道成功、失败和下一步动作。”

## 不该写进这里的内容

- 技术栈名称
- 表结构和字段设计
- 类名、函数名、文件名
- API 路径细节
- 组件拆分方案
