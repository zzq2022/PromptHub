# Tasks

- [x] 梳理 desktop rules-workspace 与 CLI 接入边界。
- [x] 在 `packages/core` 新增 shared rules workspace service。
- [x] 在 `packages/core` 新增默认平台 rules 路径辅助。
- [x] 在 `packages/core` 抽离 shared AI chat client 与 rules rewrite。
- [x] 让 desktop `rules-workspace.ts` 改为 shared service 薄封装。
- [x] 在 standalone CLI 接入 `rules` 基础命令与 `rewrite`。
- [x] 为 rules CLI 增加回归测试。
- [x] 更新 README / docs 命令表。
- [x] 运行 lint、typecheck、test、build 验证。
