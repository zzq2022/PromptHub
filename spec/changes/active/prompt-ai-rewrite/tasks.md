# Tasks

- [x] 设计 Prompt AI 改写的结构化返回协议与系统提示词。
- [x] 在 `ai.ts` 中新增 Prompt 改写服务函数。
- [x] 在 `EditPromptModal` 中接入改写状态、触发逻辑和撤销能力。
- [x] 为 Prompt 编辑界面补充最小 AI 改写 UI。
- [x] 补充 i18n 文案。
- [x] 补充组件测试覆盖成功、失败、撤销路径。
- [x] 运行桌面端相关测试与 lint 验证。
- [x] 更新 implementation.md 记录实际落地行为。
- [x] 设计非编辑态 `AI 快速编辑` 入口与交互流。
- [x] 实现共享 `PromptQuickRewriteDialog` 并复用 `rewritePromptDraft()`。
- [x] 在详情页与 `PromptDetailModal` 接入 `AI 快速编辑` 按钮。
- [x] 支持 `应用并保存` 与 `应用后继续编辑` 两条提交路径。
- [x] 为快速编辑弹窗补充生成、保存、继续编辑测试。
- [x] 收敛为共享 icon-only trigger，并在右键菜单接入同一套快速编辑弹窗。
- [x] 为 icon-only trigger 与右键菜单打开快速编辑弹窗补充回归测试。
