# Delta Spec

## Added

- 升级快照列表支持显示总数与默认可见条目数，便于用户在不展开全部历史的情况下理解当前可回滚范围。

## Modified

- 当升级快照数量较多时，设置页中的快照历史必须使用独立滚动区域，而不是无限增加页面高度。

## Removed

- 无

## Scenarios

- 当用户打开数据设置且存在 10+ 个升级快照时，默认只看到摘要和部分最新项；点击展开后，可在固定高度区域内滚动浏览全部快照并执行回滚或删除。
