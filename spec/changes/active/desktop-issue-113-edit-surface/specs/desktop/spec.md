# Delta Spec

## Modified

- Desktop 卡片详情 inline edit 状态下，标题输入框和用户提示词编辑框 SHOULD 尽量沿用只读态的原有视觉表面，只把内容区域切换为可编辑，而不是额外引入新的输入框造型。

## Scenarios

- 当用户进入桌面卡片详情 inline edit 时，标题仍保持原有排版位置，系统提示词和用户提示词仍保持原有卡片表面，只是内部文本区域变成可编辑状态。
