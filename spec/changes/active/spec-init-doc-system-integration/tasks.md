# Tasks

- [x] 把 `spec-init` skill 接入项目内 `.agents/skills/`
- [x] 在 `README.md` 增加 `spec-init` skill 链接与仓库说明
- [x] 为 `spec/` 新增 `spec-init` 边界入口目录
- [x] 更新 `AGENTS.md`，让文档工作流说明与新体系一致
- [x] 按最新 `spec-init` 结构补齐 `spec/workflow/*` 与 `spec/knowledge/*` 显式入口
- [x] 增加 `spec-init.topology.yml` 与 `spec/rules/document-routing-rules.md`
- [x] 开始第一阶段内容迁移：把核心长期真相写入 `spec/knowledge/*`
- [x] 记录本次接入后的后续迁移建议
